// import {  } from "";

export const initialState = {
  quakes: [],
  quakeFetch: false,
  quakeFetchError: false,
};

export const quakeReducer = (state = initialState, action) => {
  switch (action.type) {
    // case "":
    default:
      return state;
  }
};
